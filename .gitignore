target/
*.db
*.db-shm
*.db-wal
engine.log
# accept_operate.sh restarts the engine twice; the demo instance logs here too
engine2.log
engine3.log
demo-engine.log
.DS_Store
# accept_phase3.sh copies the two counter artifacts here
guests/counter/counter-v?.wasm
# fleet supervisor artifacts (smoke_fleet.sh / local fleets)
keel-*.log
fleet.log
fleet-smoke.toml
# smoke_dr.sh snapshot directory
smoke-dr-backups/
# smoke_providers.sh boot-refusal capture
provider-bad.log
# smoke_secrets.sh secrets fixture (never commit secret material)
smoke-secrets.env
# phase 6: trunk build output (regenerated by accept_phase6.sh; the app's
# Cargo.lock IS committed, same reproducibility policy as the root lockfile)
apps/hello/dist/
