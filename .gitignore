target/
*.db
*.db-shm
*.db-wal
engine.log
.DS_Store
# accept_phase3.sh copies the two counter artifacts here
guests/counter/counter-v?.wasm
# fleet supervisor artifacts (smoke_fleet.sh / local fleets)
keel-*.log
fleet.log
fleet-smoke.toml
# smoke_dr.sh snapshot directory
smoke-dr-backups/
# smoke_providers.sh boot-refusal capture
provider-bad.log
# smoke_secrets.sh secrets fixture (never commit secret material)
smoke-secrets.env
